<Window x:Class="WorldCup.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WorldCup.WPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <!-- Gender Combo -->
        <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Gender:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <ComboBox x:Name="cmbGender" Width="100"/>
        </StackPanel>

        <!-- Favorite Team Combo -->
        <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Favorite Team:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <ComboBox x:Name="cmbFavoriteTeam" Width="200"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="0" Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,10">
            <Button x:Name="btnLoadMatches" Content="Load Matches" Width="100" Margin="0,0,10,0"/>
            <Button x:Name="btnLoadPlayers" Content="Load Players" Width="100"/>
        </StackPanel>

        <!-- Matches List -->
        <GroupBox Header="Matches" Grid.Row="1" Grid.Column="0" Margin="0,0,10,0">
            <ListBox x:Name="lstMatches"/>
        </GroupBox>

        <!-- Players List -->
        <GroupBox Header="Players" Grid.Row="1" Grid.Column="2" Margin="10,0,0,0">
            <ListBox x:Name="lstPlayers"/>
        </GroupBox>

        <!-- Status or Info (Optional) -->
        <TextBlock Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" Text="Ready" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0,10,0,0"/>

        <GroupBox Header="Favorite Players" Grid.Row="1" Grid.Column="1" Margin="10,0">
            <ListBox x:Name="lstFavoritePlayers"/>
        </GroupBox>

        <Button x:Name="btnAddToFavorites" Content="Add to Favorites" Grid.Row="2" Grid.Column="2" Width="150" Margin="0,10,0,0" HorizontalAlignment="Right"/>

    </Grid>
</Window>
